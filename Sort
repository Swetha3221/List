'''
'''
Write a program to sort the given list and print it.
Sample Input:

30 20 10 50 40

Sample Output:

10 20 30 40 50
Ans:
# Input for the list elements
elements = input("Enter the list elements: ").split()  # Input list elements in a single line

# Convert the input list to integers
my_list = list(map(int, elements))  # Convert the list elements to integers

# Sort the list
my_list.sort()  # Sort the list in ascending order

# Output the sorted list
print(" ".join(map(str, my_list)))  # Join the sorted list into a string and print
